# Library-Management-System
Built with Python (Flask) &amp; SQLite3. Enforces strict backend-driven workflows and navigation using URL parameters instead of client-side JS. Includes optimized relational schemas with foreign key constraints across tables for users, books, loans, and automated fine calculation engines. Complete with custom CSS.
